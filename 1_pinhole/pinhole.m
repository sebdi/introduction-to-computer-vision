function [x, y] = pinhole(w, Lambda, Omega, tau)
% TODO implement the pinhole camera model
%
%w = ... % transform world point to camera coordinate system
%x = ... % compute x value in image plane
%y = ... % compute y value in image plane
end

